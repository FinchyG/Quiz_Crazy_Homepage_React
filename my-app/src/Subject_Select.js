import React from 'react';
import ReactDom from 'react-dom';
import './App.css';


export class Subject_Select extends React.Component {
    render() {
        return (
        <h4>{ this.props.title }</h4>
        );
    }
}